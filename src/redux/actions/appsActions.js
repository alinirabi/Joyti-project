
import { history } from '../../common';
import {
  APP_SUBMIT,
  APP_SUBMIT_FAIL,
  APP_INIT
} from '../ReduxTypes';


export const creatApp = (obj,mode) => {
  return dispatch => {
    dispatch({ type: "SOMETHING" });
    
  };
};
